/**
 * @swagger
 * components:
 *  schemas:
 *    Action:
 *      type: object
 *      properties:
 *        id:
 *          type: string
 *        title:
 *          type: string
 */
export interface Action {
  id: string;
  title: string;
}
